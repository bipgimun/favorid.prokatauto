<style>
    .js-editable-body .non-editable-content,
    .style-non-editable {
        display: block;
        padding-top: 7px;
        font-weight: bold;
    }

    .js-editable-body .editable-content {
        display: none;
    }

    .js-editable-body.editable-on .editable-content {
        display: block;
    }

    .js-editable-body.editable-on .non-editable-content {
        display: none;
    }

    .footer-btn-block-resrvation {
        display: flex;
    }

    .footer-resrvation-save-btn {
        margin-right: auto;
    }

    .footer-resrvation-end-btn {
        margin-left: auto;
        margin-right: 10px;
    }
</style>

<header class="page-header">
    <h2>Расход денежных средств</h2>

    <div class="right-wrapper pull-right">
        <ol class="breadcrumbs">
            <li>
                <a href="/">
                    <i class="fa fa-book"></i>
                </a>
            </li>
            <li><span>Расход</span> <span class="separator"></span></li>
        </ol>
    </div>
</header>
<section class="panel panel-featured panel-featured-primary">
    <div class="panel-body">
        <section class="panel">
            <header class="panel-heading">
                <h2 class="panel-title">Новый документ расхода</h2>
            </header>
            {{#each costs}}
                <div class="panel-body js-editable-body">
                    <form id="js-costs-add-form" action="/api/costs/update" data-target="view"
                        class="form-horizontal mb-lg" novalidate="novalidate">
                        <input type="hidden" name="id" value="{{id}}">
                        <div class="form-group">
                            <label class="col-sm-4 control-label">Дата</label>
                            <div class="non-editable-content" data-target="date">
                                {{toIsoString this.date 'DD.MM.YYYY'}}
                            </div>
                            <div class="col-sm-8 editable-content">
                                <input value="{{toIsoString this.date 'YYYY-MM-DD'}}" name="date" type="date"
                                    class="form-control" placeholder="Введите дату" required>
                            </div>
                        </div>
                        <div class="form-group">
                            <label class="col-sm-4 control-label">Основание</label>
                            <div class="col-sm-8 style-non-editable">
                                {{#if document_id}}
                                    {{base}}
                                {{else if driver_id}}
                                    <a href="/drivers/{{../driver.id}}" target="__blank">{{../driver.name}}</a>
                                {{else if customer_id}}
                                    <a href="/customers/{{../customer_id}}" target="__blank">{{customer_name}}</a>
                                {{else if supplier_id}}
                                    <a href="/suppliers/{{supplier_id}}">{{supplier_name}}</a>
                                {{else}}
                                    <p>{{base}}</p>
                                {{/if}}
                            </div>
                        </div>

                        <div class="col-sm-12 control-label" style="text-align:center;">
                            <span>--------Разбивка--------</span>
                        </div>

                        <div class="form-group">
                            <div class="col-sm-12 control-label" style="text-align:center;">
                                <span>АВТОМОБИЛИ</span>
                            </div>
                            {{#each ../grouppedDetails.auto}}
                                <div class="form-group col-sm-12">
                                    <div class="non-editable-content">
                                        <label class="col-sm-2 control-label">Авто</label>
                                        <div class="">
                                            <div class="input-group">
                                                <div class="form-control">{{name}}</div>
                                                <span class="form-control">{{price}} руб.</span>
                                            </div>
                                        </div>
                                    </div>
                                    <div class="editable-content js-detail-update">
                                        <label class="col-sm-2 control-label">Авто</label>
                                        <select class="form-control js-detail-update-target_id">
                                            <option value="" selected disabled>Выберите авто</option>
                                            <option value="-1">Удалить (строка будет удалена после сохранения документа)
                                            </option>
                                            {{#each ../../cars}}
                                                <option {{compare id ../target_id 'selected' ''}} value="{{id}}">
                                                    {{name}} {{model}} - {{number}}</option>
                                            {{/each}}
                                        </select>
                                        <input type="number" class="form-control js-detail-update-price"
                                            value="{{price}}">
                                        <input type="hidden" value="{{id}}" class="js-detail-update-id">
                                        <input type="hidden" value="auto" class="js-detail-update-target_type">
                                    </div>
                                </div>
                            {{/each}}
                            <div class="editable-content autos-group">
                                <div class="form-group col-sm-12 autos-group__item">
                                    <label class="col-sm-2 control-label">Авто</label>
                                    <div class="col-sm-10">
                                        <div class="input-group js-detail-update">
                                            <select class="form-control js-detail-update-target_id">
                                                <option value="" selected disabled>Выберете автомобиль</option>
                                                {{#each ../cars}}
                                                    <option value="{{id}}">{{name}} {{model}} - {{number}}</option>
                                                {{/each}}
                                            </select>
                                            <input type="number" class="form-control input-sm js-detail-update-price"
                                                placeholder="руб">
                                            <input type="hidden" value="auto" class="js-detail-update-target_type">
                                            <span class="input-group-btn">
                                                <button class="btn btn-default adding-item-autos"
                                                    type="button">+</button>
                                            </span>
                                            <span class="input-group-btn">
                                                <button class="btn btn-default deliting-item-autos"
                                                    type="button">-</button>
                                            </span>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="form-group">
                            <div class="col-sm-12 control-label" style="text-align:center;">
                                <span>КВАРТИРЫ</span>
                            </div>
                            {{#each ../grouppedDetails.apartments}}
                                <div class="non-editable-content form-group col-sm-12">
                                    <label class="col-sm-2 control-label">Квартира</label>
                                    <div>
                                        <div class="input-group">
                                            <div class="form-control">{{name}}</div>
                                            <span class="form-control">{{price}} руб.</span>
                                        </div>
                                    </div>
                                </div>
                                <div class="editable-content js-detail-update">
                                    <label class="col-sm-2 control-label">Квартира</label>
                                    <select class="form-control js-detail-update-target_id">
                                        <option value="" selected disabled>Выберите квартиру</option>
                                        <option value="-1">Удалить (строка будет удалена после сохранения документа)
                                        </option>
                                        {{#each ../../apartments}}
                                            <option {{compare id ../target_id 'selected' ''}} value="{{id}}">{{address}}
                                            </option>
                                        {{/each}}
                                    </select>
                                    <input type="number" class="form-control js-detail-update-price" value="{{price}}">
                                    <input type="hidden" value="{{id}}" class="js-detail-update-id">
                                    <input type="hidden" value="apartments" class="js-detail-update-target_type">
                                </div>
                            {{/each}}
                            <div class="editable-content apartments-group">
                                <div class="form-group col-sm-12 apartments-group__item">
                                    <label class="col-sm-2 control-label">Квартира</label>
                                    <div class="col-sm-10">
                                        <div class="input-group js-detail-update">
                                            <select class="form-control js-detail-update-target_id">
                                                <option value="0" selected disabled>Выберете квартиру</option>
                                                {{#each ../apartments}}
                                                    <option value="{{id}}">{{address}}</option>
                                                {{/each}}
                                            </select>
                                            <input type="number" class="form-control input-sm js-detail-update-price"
                                                placeholder="руб">
                                            <input type="hidden" value="apartments"
                                                class="js-detail-update-target_type">
                                            <span class="input-group-btn">
                                                <button class="btn btn-default adding-item-apartments"
                                                    type="button">+</button>
                                            </span>
                                            <span class="input-group-btn">
                                                <button class="btn btn-default deliting-item-apartments"
                                                    type="button">-</button>
                                            </span>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="form-group">
                            <div class="col-sm-12 control-label" style="text-align:center;">
                                <span>ВОДИТЕЛИ</span>
                            </div>
                            {{#each ../grouppedDetails.drivers}}
                                <div class="non-editable-content form-group col-sm-12">
                                    <label class="col-sm-2 control-label">Водитель</label>
                                    <div class="">
                                        <div class="input-group">
                                            <div class="form-control">{{name}}</div>
                                            <span class="form-control">{{price}} руб.</span>
                                        </div>
                                    </div>
                                </div>
                                <div class="editable-content js-detail-update">
                                    <label class="col-sm-2 control-label">Водитель</label>
                                    <select class="form-control js-detail-update-target_id">
                                        <option value="" selected disabled>Выберите водителя</option>
                                        <option value="-1">Удалить (строка будет удалена после сохранения документа)
                                        </option>
                                        {{#each ../../drivers}}
                                            <option {{compare id ../target_id 'selected' ''}} value="{{id}}">{{name}}
                                            </option>
                                        {{/each}}
                                    </select>
                                    <input type="number" class="form-control js-detail-update-price" value="{{price}}">
                                    <input type="hidden" value="{{id}}" class="js-detail-update-id">
                                    <input type="hidden" value="drivers" class="js-detail-update-target_type">
                                </div>
                            {{/each}}
                            <div class="editable-content drivers-group">
                                <div class="form-group col-sm-12 drivers-group__item">
                                    <label class="col-sm-2 control-label">Водитель</label>
                                    <div class="col-sm-10">
                                        <div class="input-group js-detail-update">
                                            <select class="form-control js-detail-update-target_id">
                                                <option value="" selected disabled>Выберете водителя</option>
                                                {{#each ../drivers}}
                                                    <option value="{{id}}">{{name}}</option>
                                                {{/each}}
                                            </select>
                                            <input type="number" class="form-control input-sm js-detail-update-price"
                                                placeholder="руб">
                                            <input type="hidden" class="js-detail-update-target_type" value="drivers">
                                            <span class="input-group-btn">
                                                <button class="btn btn-default adding-item-drivers"
                                                    type="button">+</button>
                                            </span>
                                            <span class="input-group-btn">
                                                <button class="btn btn-default deliting-item-drivers"
                                                    type="button">-</button>
                                            </span>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="form-group">
                            <div class="col-sm-12 control-label" style="text-align:center;">
                                <span>КОНТРАКТЫ</span>
                            </div>
                            {{#each ../grouppedDetails.contracts}}
                                <div class="non-editable-content form-group col-sm-12">
                                    <label class="col-sm-2 control-label">Контракт</label>
                                    <div class="">
                                        <div class="input-group">
                                            <div class="form-control">{{name}}</div>
                                            <span class="form-control">{{price}} руб.</span>
                                        </div>
                                    </div>
                                </div>
                                <div class="editable-content js-detail-update">
                                    <label class="col-sm-2 control-label">Контракт</label>
                                    <select class="form-control js-detail-update-target_id">
                                        <option value="" selected disabled>Выберите контракт</option>
                                        <option value="-1">Удалить (строка будет удалена после сохранения документа)
                                        </option>
                                        {{#each ../../contracts}}
                                            <option {{compare id ../target_id 'selected' ''}} value="{{id}}">MUZ-{{id}}
                                            </option>
                                        {{/each}}
                                    </select>
                                    <input type="number" class="form-control js-detail-update-price" value="{{price}}">
                                    <input type="hidden" value="{{id}}" class="js-detail-update-id">
                                    <input type="hidden" value="contracts" class="js-detail-update-target_type">
                                </div>
                            {{/each}}
                            <div class="editable-content contracts-group">
                                <div class="form-group col-sm-12 contracts-group__item">
                                    <label class="col-sm-2 control-label">Контракт</label>
                                    <div class="col-sm-10">
                                        <div class="input-group js-detail-update">
                                            <select class="form-control js-detail-update-target_id">
                                                <option value="" selected disabled>Выберете контракт</option>
                                                {{#each ../contracts}}
                                                    <option value="{{id}}">MUZ-{{id}}</option>
                                                {{/each}}
                                            </select>
                                            <input type="number" class="form-control input-sm js-detail-update-price" placeholder="руб">
                                            <input type="hidden" value="contracts"
                                                class="form-control input-sm js-detail-update-target_type">
                                            <span class="input-group-btn">
                                                <button class="btn btn-default adding-item-contracts"
                                                    type="button">+</button>
                                            </span>
                                            <span class="input-group-btn">
                                                <button class="btn btn-default deliting-item-contracts"
                                                    type="button">-</button>
                                            </span>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        
                        <div class="form-group">
                            <div class="col-sm-12 control-label" style="text-align:center;">
                                <span>ПРОКАТ АВТО</span>
                            </div>
                            {{#each ../grouppedDetails.carsReserv}}
                                <div class="non-editable-content form-group col-sm-12">
                                    <label class="col-sm-2 control-label">Заявка на аренду авто</label>
                                    <div>
                                        <div class="input-group">
                                            <div class="form-control">{{name}}</div>
                                            <span class="form-control">{{price}} руб.</span>
                                        </div>
                                    </div>
                                </div>
                                <div class="editable-content js-detail-update">
                                    <label class="col-sm-2 control-label">Заявка на аренду авто</label>
                                    <select class="form-control js-detail-update-target_id">
                                        <option value="" selected disabled>Выберите заявку</option>
                                        <option value="-1">Удалить (строка будет удалена после сохранения документа)
                                        </option>
                                        {{#each ../../carsReservs}}
                                            <option {{compare id ../target_id 'selected' ''}} value="{{id}}">CRR-{{id}}
                                            </option>
                                        {{/each}}
                                    </select>
                                    <input type="number" class="form-control js-detail-update-price" value="{{price}}">
                                    <input type="hidden" value="{{id}}" class="js-detail-update-id">
                                    <input type="hidden" value="carsReserv" class="js-detail-update-target_type">
                                </div>
                            {{/each}}
                            <div class="editable-content carsReservs-group">
                                <div class="form-group col-sm-12 carsReservs-group__item">
                                    <label class="col-sm-2 control-label">Заявка на аренду авто</label>
                                    <div class="col-sm-10">
                                        <div class="input-group js-detail-update">
                                            <select class="form-control js-detail-update-target_id">
                                                <option value="" selected disabled>Выберете заявку</option>
                                                {{#each ../carsReservs}}
                                                    <option value="{{id}}">CRR-{{id}}</option>
                                                {{/each}}
                                            </select>
                                            <input type="number" class="form-control input-sm js-detail-update-price" placeholder="руб">
                                            <input type="hidden" value="carsReserv" class="form-control input-sm js-detail-update-target_type">
                                            <span class="input-group-btn">
                                                <button class="btn btn-default adding-item-carsReservs"
                                                    type="button">+</button>
                                            </span>
                                            <span class="input-group-btn">
                                                <button class="btn btn-default deliting-item-carsReservs"
                                                    type="button">-</button>
                                            </span>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="form-group">
                            <label class="col-sm-4 control-label">Статья расходов</label>
                            <div class="col-sm-8">
                                <div>
                                    <div class="non-editable-content" data-target="category">
                                        {{category}}
                                    </div>
                                    <div class="editable-content">
                                        <select style="width: 100%" name="category_id" id="js-costsForm-category-view">
                                            <option value="" selected disabled>Выберите статью расхода из списка
                                            </option>
                                            {{#each ../costsCategories}}
                                                <option {{compare id ../category_id 'selected' ''}} value="{{this.id}}">
                                                    {{this.title}}</option>
                                            {{/each}}
                                        </select>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="form-group">
                            <label class="col-sm-4 control-label">Касса / счет</label>
                            <div class="col-sm-8">
                                <div>
                                    <div class="non-editable-content" data-target="cashbox_name">
                                        {{cashbox_name}}
                                    </div>
                                    <div class="editable-content">
                                        <select style="width: 100%" name="cash_storage_id"
                                            id="js-costsForm-cashStorages-view">
                                            <option value="" selected disabled>Выберите кассу / счёт из списка</option>
                                            {{#each ../cashStorages}}
                                                <option {{compare id ../cash_storage_id 'selected' ''}} value="{{id}}">
                                                    {{name}}</option>
                                            {{/each}}
                                        </select>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="form-group">
                            <label class="col-sm-4 control-label">Сумма</label>
                            <div class="non-editable-content" data-target="sum">
                                {{sum}}
                            </div>
                            <div class="col-sm-8 editable-content">
                                <input value="{{sum}}" type="number" name="sum" class="form-control">
                            </div>
                        </div>
                        <div class="form-group">
                            <label class="col-sm-4 control-label">Менеджер</label>
                            <div class="style-non-editable">{{manager_name}}</div>
                        </div>
                        <div class="form-group">
                            <label class="col-sm-4 control-label">Комментарий</label>
                            <div class="non-editable-content" data-target="comment">
                                {{comment}}
                            </div>
                            <div class="col-sm-8 editable-content">
                                <textarea value="{{comment}}" name="comment" cols="20" rows="4" class="form-control"
                                    placeholder="Введите комментарий" required>{{comment}}</textarea>
                            </div>
                        </div>
                        <div class="col-md-12 text-right editable-content">
                            <div class="footer-btn-block-resrvation">
                                <button class="btn btn-success footer-resrvation-save-btn">Сохранить</button>
                            </div>
                        </div>
                    </form>
                </div>
            {{/each}}
            <div class="footer-btn-block-resrvation">
                <a class="btn" href="/api/costs/downloadPrint/{{id}}">Печать</a>
                {{#unless item.completed}}
                    <button class="btn btn-primary js-toggleEditable">Редактировать</button>
                    <button class="btn btn-danger footer-resrvation-end-btn">Завершить</button>
                    <button data-target="costs" data-id="{{id}}" class="btn btn-danger js-tableEssence-remove">Удалить
                        документ</button>
                {{/unless}}
            </div>
        </section>
    </div>
</section>
<script>
    var btnAddProduct = document.querySelector('.adding-item-autos');
    var btnDeleteProduct = document.querySelector('.deliting-item-autos');
    var table = document.querySelector('.autos-group');

    function insRow() {
        var target = event.target;
        var grItem = table.querySelectorAll('.autos-group__item');
        var new_row = grItem[0].cloneNode(true);
        new_row.querySelector('.adding-item-autos').addEventListener('click', insRow);

        var dateInp = new_row.querySelectorAll('input');
        for (var i = 0; i < dateInp.length; i++) {
            if (dateInp[i].className.indexOf('js-detail-update') > -1) {
                continue;
            }

            dateInp[i].value = '';
        }

        table.appendChild(new_row);
    }

    function delRow() {
        var target = event.target;
        var closeBtn = target.closest('.deliting-item-autos');
        if (!closeBtn) return;

        var grItem = table.querySelectorAll('.autos-group__item');
        var actualTbody = grItem.length;
        if (actualTbody == 1) {

            $('.autos-group__item').find('input, select').each((index, element) => {
                const $element = $(element);

                if(!['js-detail-update-id', 'js-detail-update-target_type'].includes($element.get(0).className)) {
                    $element.val('');
                }
            })

            return;
        } else {
            indexRow = closeBtn.parentNode.parentNode.parentNode.parentNode.remove();
        }
    }

    btnAddProduct.addEventListener('click', insRow);

    table.addEventListener('click', delRow);
</script>
<script>
    var btnAddProductApart = document.querySelector('.adding-item-apartments');
    var btnDeleteProductApart = document.querySelector('.deliting-item-apartments');
    var tableApart = document.querySelector('.apartments-group');

    function insRowApart() {
        var targetApart = event.target;
        var grItemApart = tableApart.querySelectorAll('.apartments-group__item');
        var new_rowApart = grItemApart[0].cloneNode(true);
        new_rowApart.querySelector('.adding-item-apartments').addEventListener('click', insRowApart);

        var dateInpApart = new_rowApart.querySelectorAll('input');
        for (var i = 0; i < dateInpApart.length; i++) {
            if (dateInp[i].className.indexOf('js-detail-update') > -1) {
                continue;
            }

            dateInpApart[i].value = '';
        }

        tableApart.appendChild(new_rowApart);
    }

    function delRowApart() {
        var targetApart = event.target;
        var closeBtnApart = targetApart.closest('.deliting-item-apartments');
        if (!closeBtnApart) return;

        var grItemApart = tableApart.querySelectorAll('.apartments-group__item');
        var actualTbodyApart = grItemApart.length;
        if (actualTbodyApart == 1) {
             $('.apartments-group__item').find('input, select').each((index, element) => {
                const $element = $(element);

                if(!['js-detail-update-id', 'js-detail-update-target_type'].includes($element.get(0).className)) {
                    $element.val('');
                }
            })
            return;
        } else {
            indexRowApart = closeBtnApart.parentNode.parentNode.parentNode.parentNode.remove();
        }
    }

    btnAddProductApart.addEventListener('click', insRowApart);

    tableApart.addEventListener('click', delRowApart);
</script>
<script>
    var btnAddProductDrivers = document.querySelector('.adding-item-drivers');
    var btnDeleteProductDrivers = document.querySelector('.deliting-item-drivers');
    var tableDrivers = document.querySelector('.drivers-group');

    function insRowDrivers() {
        var targetDrivers = event.target;
        var grItemDrivers = tableDrivers.querySelectorAll('.drivers-group__item');
        var new_rowDrivers = grItemDrivers[0].cloneNode(true);
        new_rowDrivers.querySelector('.adding-item-drivers').addEventListener('click', insRowDrivers);

        var dateInpDrivers = new_rowDrivers.querySelectorAll('input');
        for (var i = 0; i < dateInpDrivers.length; i++) {
            if (dateInp[i].className.indexOf('js-detail-update') > -1) {
                continue;
            }

            dateInpDrivers[i].value = '';
        }

        tableDrivers.appendChild(new_rowDrivers);
    }

    function delRowDrivers() {
        var targetDrivers = event.target;
        var closeBtnDrivers = targetDrivers.closest('.deliting-item-drivers');
        if (!closeBtnDrivers) return;

        var grItemDrivers = tableDrivers.querySelectorAll('.drivers-group__item');
        var actualTbodyDrivers = grItemDrivers.length;
        if (actualTbodyDrivers == 1) {
             $('.drivers-group__item').find('input, select').each((index, element) => {
                const $element = $(element);

                if(!['js-detail-update-id', 'js-detail-update-target_type'].includes($element.get(0).className)) {
                    $element.val('');
                }
            })
            return;
        } else {
            indexRowDrivers = closeBtnDrivers.parentNode.parentNode.parentNode.parentNode.remove();
        }
    }

    btnAddProductDrivers.addEventListener('click', insRowDrivers);

    tableDrivers.addEventListener('click', delRowDrivers);
</script>
<script>
    var btnAddProductContr = document.querySelector('.adding-item-contracts');
    var btnDeleteProductContr = document.querySelector('.deliting-item-contracts');
    var tableContr = document.querySelector('.contracts-group');

    function insRowContr() {
        var targetContr = event.target;
        var grItemContr = tableContr.querySelectorAll('.contracts-group__item');
        var new_rowContr = grItemContr[0].cloneNode(true);
        new_rowContr.querySelector('.adding-item-contracts').addEventListener('click', insRowContr);

        var dateInpContr = new_rowContr.querySelectorAll('input');
        for (var i = 0; i < dateInpContr.length; i++) {
            if (dateInp[i].className.indexOf('js-detail-update') > -1) {
                continue;
            }

            dateInpContr[i].value = '';
        }

        tableContr.appendChild(new_rowContr);
        initServiceSelect();
    }

    function delRowContr() {
        var targetContr = event.target;
        var closeBtnContr = targetContr.closest('.deliting-item-contracts');
        if (!closeBtnContr) return;

        var grItemContr = tableContr.querySelectorAll('.contracts-group__item');
        var actualTbodyContr = grItemContr.length;
        if (actualTbodyContr == 1) {
             $('.contracts-group__item').find('input, select').each((index, element) => {
                const $element = $(element);

                if(!['js-detail-update-id', 'js-detail-update-target_type'].includes($element.get(0).className)) {
                    $element.val('');
                }
            })
            return;
        } else {
            indexRowContr = closeBtnContr.parentNode.parentNode.parentNode.parentNode.remove();
        }
    }

    btnAddProductContr.addEventListener('click', insRowContr);

    tableContr.addEventListener('click', delRowContr);
</script>
<script>
    var btnAddProductContr = document.querySelector('.adding-item-contracts');
    var btnDeleteProductContr = document.querySelector('.deliting-item-contracts');
    var tableContr = document.querySelector('.contracts-group');

    function insRowContr() {
        var targetContr = event.target;
        var grItemContr = tableContr.querySelectorAll('.contracts-group__item');
        var new_rowContr = grItemContr[0].cloneNode(true);
        new_rowContr.querySelector('.adding-item-contracts').addEventListener('click', insRowContr);

        var dateInpContr = new_rowContr.querySelectorAll('input');
        for (var i = 0; i < dateInpContr.length; i++) {
            if (dateInp[i].className.indexOf('js-detail-update') > -1) {
                continue;
            }

            dateInpContr[i].value = '';
        }

        tableContr.appendChild(new_rowContr);
        initServiceSelect();
    }

    function delRowContr() {
        var targetContr = event.target;
        var closeBtnContr = targetContr.closest('.deliting-item-carsReservs');
        if (!closeBtnContr) return;

        var grItemContr = tableContr.querySelectorAll('.carsReservs-group__item');
        var actualTbodyContr = grItemContr.length;
        if (actualTbodyContr == 1) {
             $('.carsReservs-group__item').find('input, select').each((index, element) => {
                const $element = $(element);

                if(!['js-detail-update-id', 'js-detail-update-target_type'].includes($element.get(0).className)) {
                    $element.val('');
                }
            })
            return;
        } else {
            indexRowContr = closeBtnContr.parentNode.parentNode.parentNode.parentNode.remove();
        }
    }

    btnAddProductContr.addEventListener('click', insRowContr);

    tableContr.addEventListener('click', delRowContr);
</script>